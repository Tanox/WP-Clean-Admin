# 2026-09-13

## WP Clean Admin：全量同步项目文档与规范文件（至 1.8.5）

- 背景：仓库在飞版本为 1.8.5（`wpcleanadmin/wp-clean-admin.php` 的 `WPCA_VERSION` 常量、插件头 `Version:`、README、根 CHANGELOG 均 1.8.5），但 openspec 规范文件多处仍写 `1.8.3`。
- 本次「更新所有项目文档和规范文件」有界范围：
  - 版本同步（描述性"当前版本"引用 → 1.8.5）：`openspec/AGENTS.md` 项目概述、`openspec/specs/plugin-architecture/detailed-design.md`（项目概述 / 示例 `WPCA_VERSION` 常量 / 两处 `@version` 类注释 / 示例插件头 `Version:`）、`openspec/specs/api/spec.md` 当前 API 版本。
  - 修正规范示例中过时署名：`detailed-design.md` 示例插件头 `Author: Sut`→`Tanox`、仓库 `sutchan`→`Tanox`（与 1.8.5 作者统一一致）。
  - 清理规范补记：`openspec/specs/admin-cleanup/spec.md` 相关文件新增 `class-wpca-cleanup-media-task.php`（媒体清理任务类，复用 Cleanup_Helpers trait）。
  - 根 `CHANGELOG.md` 的 `[1.8.5]` Docs 条目补记上述规范同步（沿用当天在飞版本，未再 bump）。
- 明确保留不动的历史引用：`design.md`「1.8.3 确立/前」、`detailed-design.md`「见 1.8.3 修复记录」、`api/spec.md` 历史版本表、`changes/plugin-prototype/*` 提案基线、`openspec/CHANGELOG.md`、`project.md` 历史条目。
- 原型 `prototype/` 无插件版本串（仅演示数据中的 PHP/WP/MySQL 版本），无需同步。
- 过时工具 `update_versions.php` / `update_versions.ps1` 仍硬编码 1.8.0→1.8.1，未改（属工具脚本，非文档/规范；如需下次发版可用，应改为当前基线）。

## 待办/注意
- `wpcleanadmin/includes/class-wpca-cleanup-media-task.php` 仍未被实例化引用（无 `new Cleanup_Media_Task`），属抽离重构模块，规范仅补记文件，未宣称已接入。
- README.en.md 尚未同步 README 中文版的配图（assets/hero-banner.svg、assets/features.svg）。
