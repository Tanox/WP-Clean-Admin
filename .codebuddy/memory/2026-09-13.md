# 2026-09-13

## 原型重构（prototype 目录整理 + 还原真实 WP 后台界面）

- 旧原型在 `prototype/ui/` 下，使用自定义顶栏/侧栏，与真实 WP 插件界面脱节。已清空并重建：原型文件上移到 `prototype/` 根目录，结构为 `index.html` + `wpca-admin.css`（WP 后台外壳：#wpadminbar/#adminmenu/.wrap/.form-table/.button/.notice）+ `wpca-components.css`（插件专属：选项卡导航/菜单定制树/顺序列表）+ `app.js`（选项卡切换/菜单树展开折叠/全选/拖拽排序/保存演示）。
- 还原对象：真实插件仅注册一个「设置 → Clean Admin」页面（`class-wpca-settings.php` 的 `add_options_page`），含 5 个选项卡（常规/清理/性能/安全/诊断）与真实字段（见 `includes/settings/fields/*` 与 `class-wpca-settings-registration-tasks.php` 的 section/field 标题）。清理选项卡含菜单定制树（真实 `menu-customization.php` 渲染）。
- 版本：原型无版本串，本次纯设计改动不 bump 插件版本（遵循 MEMORY「原型无版本串」约定）。

## CI 第二轮修复（1.8.9）：php-lint / phpunit 阻塞项

CI 在 composer 修复（1.8.8 已提交推送）后暴露真实缺陷，已全部修复：

- `performance-ajax.php`：WP 函数调用处双反斜杠 `\\wp_send_json_error` / `\\__` 非法语法（被先前已删的 IDE stub 声明掩盖，stub 删除后才暴露）。用脚本把全文 `\\`→`\`（14 处）。
- 删除 `tests/PrototypeTest.php`：其 `setUp()` 仍 require 已被清理的 `prototype/php/` 目录，PHPUnit 致命错误。
- 重写 `tests/bootstrap.php`：提前 `define('ABSPATH')` 阻止 autoloader 加载空白 `wpca-wordpress-stubs.php`，提供最小 WP 函数 polyfill（`sanitize_text_field` 正确实现：去 script/style + strip_tags + trim），并显式 `require wpca-core-functions.php`（定义 wpca_get_settings/version/text_domain 等）。使单元测试在无 WP 环境下可运行。
- `phpcs.xml.dist` 增加 `installed_paths`（vendor/wp-coding-standards/wpcs 等），修复 "Referenced sniff does not exist"（WordPress 标准此前未注册）。phpcs 步骤为 continue-on-error（非阻塞）。
- 版本 bump 1.8.8 → 1.8.9（主文件 `Version:`/`WPCA_VERSION`、README、CHANGELOG）。

### 架构认知（长期）
- `includes/autoload.php`：当 `!defined('ABSPATH')` 时 `require wpca-wordpress-stubs.php`（空实现 IDE stub）并 define ABSPATH。bootstrap 提前 define ABSPATH 即可跳过加载 stub。
- `includes/wpca-core-functions.php` 顶部 `if (!defined('ABSPATH')) exit;`：bootstrap define ABSPATH 后才能安全 require。
- 单元测试在无 WP 环境运行：依赖 WP 函数的代码用 `function_exists` 守卫或 bootstrap polyfill。
- 本机无 PHP 环境，无法本地 lint/phpunit，只能靠 CI 验证。

## CI 第三轮修复（仍属 1.8.9，未发布轮次）：php-lint 深层语法错误

上一轮 1.8.9 初步修复（performance 双反斜杠、删 PrototypeTest、bootstrap、phpcs、bump）被提交后，CI 的 `php -l` 在 `includes/modules/core/classes/class-wpca-core.php` 报新错 `Namespace declaration statement has to be the very first statement`——此前被 performance-ajax 的 abort 掩盖，modules/ 从未被完整 lint 过。

- 根因：`modules/` 下 12 个类文件在 `namespace` 声明**之前**写了 `require_once __DIR__ . '/...';` 依赖加载（modules 重构时引入的普遍模式）。PHP 要求 namespace 必须是首个语句（除 declare/注释）。
- 修复：脚本把每个文件 namespace 前的 `require/include` 整体移至 `namespace` 之后（保留原始 CRLF/LF 行尾，避免 diff 膨胀）。涉及 `modules/admin/classes/*`(7)、`modules/admin/settings/*`(3)、`modules/core/classes/*`(2)。
- 全仓库搜 `^function\s+\\`（全限定 `function \wp_...() {}` 非法声明）= 0 结果：确认主文件/performance 删除 stub 后已无残留；modules 文件的 "Declare WordPress functions for IDE compatibility" stub 用的是合法形式（`function wpca_get_settings() {}` 无反斜杠），语法合法、不阻塞 lint，本轮未删（避免扩大改动面）。
- 同步上述 12 个 modules 文件 + performance-ajax.php 头注释 `@version` → 1.8.9（遵守版本同步规则）。
- CHANGELOG 1.8.9 节补充 modules 命名空间修复条目。版本保持 1.8.9（同一未发布修复轮次，不二次 bump）。

## CI 第四轮修复（仍属 1.8.9）：trait 常量 PHP 版本不兼容

core.php namespace 修复提交后，CI `php -l` 继续扫描暴露新错：`class-wpca-error-levels.php` 第 28 行 `trait Error_Levels` 内 `const LOG_LEVELS`（PHP < 8.2 禁止 trait 常量，WP 插件需兼容 7.4+），CI PHP 版本较低 fatal。此前被前一轮 namespace 错误 abort 掩盖。

- 修复：trait 内 `const LOG_LEVELS = array(...)` → `public static $LOG_LEVELS = array(...)`（向后兼容 PHP 7.4）。使用类 `class-wpca-error-config.php`（`use Error_Levels;`）的 4 处 `self::LOG_LEVELS` → `self::$LOG_LEVELS`。
- 全仓库仅 1 个 trait 文件（error-levels），搜 `enum`/`readonly` 为 0，无其它 8.1/8.2 不兼容语法。
- `includes/class-wpca-error-handler.php` 的 `const LOG_LEVELS` 是其自身类内常量（PHP<8.2 合法），未改动。
- 同步 error-levels/error-config 头注释 `@version` → 1.8.9。版本保持 1.8.9（同一未发布 CI 修复轮次）。

## CI 第五轮修复（仍属 1.8.9）：主文件 array/isset 语言结构误用

error-levels/config 提交后，CI `php -l` 继续扫描暴露新错：`wp-clean-admin.php` 第 143 行起把 `array()` 语言结构误写成 `\array(`（带 `\` 命名空间前缀），PHP 将其当作**函数调用**，而函数参数中不能出现 `'key' => value` 数组语法 → 第 144 行 `syntax error, unexpected token "=>", expecting ")"`。此前被前序文件 abort 掩盖。

- 修复：全文件 10 处 `\array(` → `array()`（120/143/144/149/154/158/164/174/175/221）。其中 174/175/120/221 虽 lint 不报（`php -l` 只看语法），但运行期会 fatal（Call to undefined function array()），一并修。
- 同类型：`\isset(` 也是语言结构，第 123 行误加 `\` 前缀（运行期 fatal），改为 `isset()`。
- 全仓库搜 `\array(`/`\isset(`/`\empty(`/`\list(`/`\eval(` 语言结构误用，仅主文件有，已清除。
- 主文件 `Version:`/`WPCA_VERSION` 已 1.8.9，保持。

## CI 第六轮修复（仍属 1.8.9）：phpcs 依赖缺失 + phpunit 常量未定义

`php -l` 通过后被 CI 推进到 phpcs/phpunit 阶段，暴露两类问题（之前被语法 abort 一直掩盖）：

### phpcs：Referenced sniff does not exist（连锁）
- 根因：`phpcs.xml.dist:41` 的 `installed_paths` 引用无效目录 `vendor/phpcsstandards/universal`（phpcsutils 包目录名是 `phpcsutils`），且 `vendor/phpcsstandards/modernize` 对应包未声明；WordPress 标准内部引用 Universal/NormalizedArrays/Modernize，未注册则连锁报 does not exist。CI `ci.yml:42` 的 `--config-set installed_paths ".../wpcs"` 被 ruleset 项目级 config 覆盖，无效。
- 修复：`composer.json` require-dev 显式加 `phpcsstandards/phpcsutils`、`phpcsstandards/normalizedarrays`、`phpcsstandards/modernize`（均 ^1.0）；`phpcs.xml.dist` 删无效 `universal` 路径。无 composer.lock，CI `composer install` 会解析安装新依赖。

### phpunit：Undefined constant WPCA_TEXT_DOMAIN
- 根因：`class-wpca-helpers.php:526` 用 `\WPCA_TEXT_DOMAIN`（全局常量，由主文件 `wp-clean-admin.php:27` define）；但 `tests/bootstrap.php` 只加载 autoloader + `wpca-core-functions.php`，未 define 该常量。
- 修复：`tests/bootstrap.php` 在 ABSPATH define 后补 `if(!defined('WPCA_TEXT_DOMAIN')) define('WPCA_TEXT_DOMAIN','wp-clean-admin');`。

- `phpcs` job 为 `continue-on-error: true`（非阻塞）；`phpunit` 阻塞。修复后整体 CI 应绿（phpcs 可能有风格违规但非阻塞；若需完全干净后续再处理）。

## CI 第七轮补充：bootstrap 已 commit 未推送 + helpers 双保险

第六轮修的 `tests/bootstrap.php` 加了 `WPCA_TEXT_DOMAIN` define 已 commit 但**未推送**，CI 仍跑旧代码故 `Undefined constant`；phpcs 修复已推送故错误消失（git status 印证：composer/phpcs/bootstrap 已 clean=已 commit，而 CI 结果说明 bootstrap 那次 commit 未 push）。

- 双保险：`class-wpca-helpers.php:526` 的 `\WPCA_TEXT_DOMAIN` 改为 `defined('WPCA_TEXT_DOMAIN') ? \WPCA_TEXT_DOMAIN : 'wp-clean-admin'`，即使 bootstrap 未加载也能通过；同步 helpers 头 `@version` 1.8.5 → 1.8.9。
- 根因仍是 bootstrap 漏推送：`WPCA_TEXT_DOMAIN` 在代码中被引用 275+ 处，生产由主文件 define 安全，测试须靠 bootstrap 定义；长远必须推送 bootstrap.php（否则其他测试方法触发更多引用会再次 fatal）。
- 版本保持 1.8.9。

### 待办（后续）
- phpcs 仍可能因 `Universal`/`NormalizedArrays`/`Modernize` 等标准未通过 Composer 安装而报 sniff ERROR（非阻塞）。若需 phpcs 完全绿，可在 composer.json require-dev 添加 `phpcsstandards/universal` 等并更新 composer.lock。
- 上次（1.8.8）遗留：cleanup 主类接线、~43 个超 200 行文件未拆分。

## 核心类拆分（1.8.10）：Cleanup/Performance/Extension_API/Login

按"单文件 >200 行须拆分"规则，试点拆分最大的 4 个核心类（用户选定"试点最大 4 个"）：

- 模式：主类保留单例 + `use` 业务 trait，公开方法契约不变；trait 命名**无下划线驼峰**以匹配 autoload 路径解析（含下划线会被解析为双连字符路径 fatal）。
- Cleanup（889→121）：拆为 CleanupDatabaseTasks / MediaTasks / CommentsTasks / ContentTasks（4 个 trait）；主类保留 4 个 WP wrapper。
- Performance（835→105）：拆为 DisableTasks / DatabaseTasks / CacheTasks / ResourceTasks / PreloadTasks（5 个 trait）；主类保留 require stub + init。
- Extension_API（791→180）：拆为 RegistrationTasks / HookTasks / MenuTasks / SettingsTasks（4 个 trait）；末尾 6 个全局 helper 函数保留在主类文件（trait 不能含顶层函数）。
- Login（786→116）：拆为 CaptchaTasks / TwoFactorTasks / CustomizationTasks / AttemptRestrictionTasks（4 个 trait）；顶部 WP IDE stub 声明保留在主类。
- 新建 17 个 trait 文件，4 个主类均 <200 行；提交 4974837，+3291/-2853。版本 bump 1.8.9→1.8.10。

### 待办（后续）
- 4 个 trait 仍 >200 行（ContentTasks 333 / ResourceTasks 392 / ExtensionRegistrationTasks 359 / TwoFactorTasks 314），属单一职责子模块，按规则可进一步拆（未做）。
- 其余 ~33 个超 200 行文件批量拆分（排除 stubs/过程式/主入口）。
- openspec 规范文档版本引用历史脱节（停在更早版本），发版未全量同步，需专项处理。
- 本机无 PHP，拆分正确性靠 CI 验证（php -l / phpunit）。

## Settings 拆分（1.8.11）：暴露 modules 双体系（关键）

- Settings（707→130 主类）拆为 SettingsRegistrationTasks / SettingsPageRenderTasks 两个 trait；主类保留单例/初始化/子模块 require。提交 `05a5c3c`（6 文件，+725/-615）。
- **重大发现：`includes/modules/` 是活的平行类体系**，非死代码。autoload.php:67-82 支持 `WPCleanAdmin\Modules\*` → `includes/modules/`。即 `includes/class-wpca-X.php`(ns `WPCleanAdmin\X`) 与 `includes/modules/**/class-wpca-X.php`(ns `WPCleanAdmin\Modules\...\X`) 是**两套并行活类**，行数常对应（helpers/cache/resources 614/536/356 各两份），但 `modules/admin/classes/class-wpca-login.php` 仍是未拆旧 324 行副本（includes 根 Login 已拆 116）。
- 重新核算 41 个 >200 文件：includes 根活类(~15) + modules 平行类(~16) + 4 个仍>200 的 trait + stubs(775)/核心函数(371)/主入口(238)/AJAX(3)/settings 子文件(337) 等排除项。批量推进须分别处理两套体系。
- 渲染类 trait 因内联 CSS/JS 仍 >200（SettingsPageRenderTasks 421），彻底达标需抽离 `assets/css|js` 并改 enqueue，列为专项（不阻塞批量类拆分）。

### 后续策略
- 聚焦 includes 根活类批量拆（diagnostics 626 / helpers 614 / cache 536 / menu-customizer 534 / menu-manager 423 / database 420 / user-roles 417 / resources 356 / reset 323 / error-handler 321 / dashboard 282 / permissions 255 / core 236 / ajax 207），每批提交并 bump 版本。
- modules 双体系建议单独一轮，先确认是否仍活跃/是否需与 includes 根去重，再批量拆。

## 开发任务清单文档（1.8.13）

- 新增根 `TASKS.md`：汇总代码治理（41 个 >200 行文件分类：活业务18/渲染trait6/双体系副本16/排除stubs1）、功能增强提案 T001-T016 待办、plugin-prototype 归档 2 项、测试与静态分析目标、双体系去重决策项 E-1。
- 标注进行中遗留：`class-wpca-helpers.php` 存在未提交拆分改动（工作区 dirty），列为 A-0 待收尾。
- 版本 bump 1.8.12→1.8.13（主文件/README/CHANGELOG）。提交 79d04e8。

## 批量拆分推进（1.8.14 / 1.8.15）

- 1.8.14 `class-wpca-helpers.php`（613→88）：拆为 HelpersFormatTasks / HelpersEnvTasks / HelpersResponseTasks / HelpersErrorHandlingTasks / HelpersLogTasks 五 trait，保留 WPCA_Errors 抽象类；吸收 1.8.9 遗留的 WPCA_TEXT_DOMAIN 双保险（原 dirty 改动，非拆分半成品）。提交 08c03c2。
- 1.8.15 `class-wpca-diagnostics.php`（626→68）：拆为 DiagnosticsRegistrationTasks / RunnerTasks / ServerCheckTasks / ConflictCheckTasks / SecurityCheckTasks 五 trait，主类保留单例 + get_checks/get_categories。提交 0b2e65a。
- 1.8.16 `class-wpca-cache.php`（536→105）：拆为 CacheApiTasks / CacheMemoryTasks / CacheDatabaseTasks / CacheFileTasks 四 trait，主类保留单例 + 属性 + init/load_cache_settings。提交 ed23dc0。
- 1.8.18 `class-wpca-database.php`（420→66）：拆为 DatabaseInfoTasks / DatabaseBackupTasks / DatabaseRestoreTasks / DatabaseBackupListTasks 四 trait。提交 52cf07a。1.8.19 同步 TASKS.md（提交见 log）。
- **关键发现（1.8.19）**：含下划线类名（`Menu_Customizer`/`Menu_Manager`/`User_Roles`/`Error_Handler`/`Extension_API`）autoload 不可达——autoload 规则对下划线+驼峰叠加产出双连字符（`menu--customizer`），与实际文件名单连字符不匹配；`core.php`/`module-loader` 用 `class_exists`/`safe_init` 静默加载不报错，故这些根版大文件疑似**死代码**。拆分前须先决策 E-1（改 autoload 规则 or 统一类名）。无下划线类可达活跃，可正常拆。
- **CI phpcs 依赖修复（1.8.20）**：CI `composer install` 报 `phpcsstandards/normalizedarrays`、`phpcsstandards/modernize` 找不到（exit 2）。这两包不存在，NormalizedArrays/Modernize 标准由 **`phpcsstandards/phpcsextra`** 提供。已改 composer.json（换成 phpcsextra ^1.0）+ ci.yml installed_paths（wpcs/phpcsutils/phpcsextra 三路径）。提交 a5ea06c。
- 模式确认：trait 名无下划线驼峰；主类 `use` 组合；方法契约不变；各 trait ≤200 行（最大 diagnostics conflict-check 180）。本机无 PHP，正确性靠 CI php -l 验证。

## 1.8.20 phpcs 依赖修复已被推送（本轮收尾）

- 复核：a5ea06c（composer.json → `phpcsstandards/phpcsextra: ^1.0`；ci.yml installed_paths → wpcs/phpcsutils/phpcsextra）**早已本地提交但未推送**，`git status -sb` 显示 `main...origin/main [ahead 1]`，远端 HEAD 仍是 9ea2edf → CI 报的 exit 2 是旧代码所致。
- 已 `git push origin main`（9ea2edf..a5ea06c），修复生效，无需再改代码。
- **排查教训（重要）**：本会话中 `read_file`/`search_content` 返回了 `wpcleanadmin/composer.json` 的**过期缓存内容**（仍显示 normalizedarrays/modernize），与 HEAD 不一致；改用 `Get-Content`（PowerShell）与 `git show HEAD:<path>` 才看到真实内容。**涉及"文件现状"的判断，必须以 `git show HEAD:<path>` 或命令行读取磁盘为准，不能只信 read_file 缓存。**
- 另确认 `phpcs.xml.dist` 无 `installed_paths` 残留（不会覆盖 CI 的 `--config-set`）。
- 版本约束校验（无本地 composer，靠 packagist 元数据）：`phpcsstandards/phpcsextra ^1.0` 最新 1.5.1，需 phpcs `^3.13.5 || ^4.0.1` + phpcsutils `^1.2.3`，与根约束 `squizlabs/php_codesniffer ^3.6`、`phpcsutils ^1.0`、`wpcs ^3.0` 兼容，解析无冲突。

## 1.8.20 收尾：乱码提交信息已强制修正（用户确认）

- 提交记忆文档时 `git commit -m "中文"` 被 PowerShell(GBK) 破坏 → 远端出现乱码提交 `fcdf1de`（同终端下与 `-F` 版显示截然不同，可据此判别）。
- 已 amend 为内容相同的 `81d3453`（正确 UTF-8 信息），并按用户确认 `git push --force-with-lease origin main` 覆盖（`fcdf1de...53f5fb4 forced update`）；远端历史不再含乱码信息，本地与远端已同步（`main...origin/main` 无 ahead/behind）。
- 后续提交中文信息一律：`write_to_file` 到 `.git/cb-msg*.txt`（UTF-8）+ `git commit -F`。
- 遗留：`.git/cb-msg.txt`、`.git/cb-msg2.txt` 两个临时消息文件未删（在 `.git` 内，不参与版本控制）。
- CI 结论：`origin/main` 已含 `phpcsstandards/phpcsextra` 修复，`composer install` 应可解析，CI 预期恢复（phpcs 仍 `continue-on-error`，phpunit 5 个 PHP 版本矩阵）。

## 全量项目审查（1.8.20）：发现 P0 运行期致命错误 + 安全缺陷

只读审查 123 个 PHP / 22,234 行 / 38 个 >200 行文件。核心结论：

### P0 插件运行即 fatal（静态推导，必现；本机无 PHP 无法实机验证）
- **根因**：`includes/modules/admin/classes/**` 与 `includes/modules/utilities/classes/**` 共 35 个文件的 `namespace` 全部误写成根命名空间 `WPCleanAdmin`（只有 `modules/core/**`、`modules/admin/settings/**` 用了正确子命名空间），与 `includes/class-wpca-*.php` 的同名类**FQCN 完全相同**。
- **触发链**：`Module_Loader::load_legacy_modules()`（modules/core/classes/class-wpca-module-loader.php:45-69）先按 `WPCleanAdmin\{Module}` 载入根版；紧接 `load_modular_modules()`（:74-104）按 `WPCleanAdmin\Modules\Admin\Classes\{Module}` 载入副本 → autoload 命中 `includes/modules/admin/classes/class-wpca-dashboard.php` → 该文件 :23 **无守卫**再声明 `class Dashboard`（namespace `WPCleanAdmin`）→ `Cannot declare class WPCleanAdmin\Dashboard, because the name is already in use`（E_COMPILE_ERROR，`catch (\Exception)`/`catch (\Throwable)` 均拦不住，`safe_init` 无效）。
- 首次必炸点 = admin_modules 循环首项 `Dashboard`（core.php:93-94 顺序调用）。
- **第二处独立 fatal**：`includes/modules/utilities/classes/class-wpca-helpers.php:18` `require_once __DIR__ . '/wpca-wordpress-stubs.php'` —— 该目录无此文件（全仓库仅 `includes/wpca-wordpress-stubs.php`）→ `Failed opening required`。php -l 不报。

### 修正此前记忆（重要）
早先「含下划线类 = 死代码」结论需细化：根版 5 个大文件确实不可达（autoload 双连字符），但这些 FQCN 由 **modules 副本**成功声明（legacy 载入失败 → 无冲突 → modular 载入成功）。故功能是活的（由 modules 那份实现），**根版那份才是死代码**。

### P1 安全
- **2FA 可完全绕过**：`includes/class-wpca-login-two-factor-tasks.php:276-294` 的 `elseif` 分支只挂 `login_redirect` 过滤器就 `return $user;`，未验码即放行登录（root `WPCleanAdmin\Login` 是活路径，:60-63 `use LoginTwoFactorTasks`）。
- `includes/modules/admin/classes/class-wpca-menu-customizer.php:110-115` 注册的 4 个 `wp_ajax_wpca_{save,reset,export,import}_menu_customizer` 处理器无 nonce/`current_user_can`（该 FQCN 恰好不冲突 → P0 修好后即暴露）。
- 数据库恢复 SQL"白名单"仅前缀正则（class-wpca-database-restore-tasks.php:33-53）；备份写入 web 可访问的 `WPCA_PLUGIN_DIR.'backups/'`。
- 多处整包 `update_option` 无字段白名单（menu-manager/database/performance/settings-ajax）。
- 已查无发现：SQL 注入、命令执行、eval、请求可控的动态回调/反序列化；输出转义基本到位（仅 `class-wpca-error-handler.php:142-147` 在 WP_DEBUG 下未转义回显异常）。

### P2 工程与流程
- **CI 从不加载插件**：php -l 只看语法，3 个测试文件共 ~134 行且不 boot 主文件 → P0 类致命错误 CI 永远发现不了。建议加「冒烟加载检查」（定义 ABSPATH 后 require 主文件并触发 init）。
- 44 个重复全局函数名，主要来自 `wpca-wordpress-stubs.php`(775 行) 与各类文件内联 `if(!function_exists()) function xx(){}` IDE stub（生产代码反模式）。
- 声明 `Requires PHP: 7.0` 与实际不符（`: void`/`?int`/标量类型 → ≥7.1）。
- 无 `composer.lock`（构建不可复现）；phpcs `continue-on-error: true` + 12 条豁免 + 仅回开 3 条安全规则；无 PHPStan。
- 版本号通胀：一天内 1.8.10→1.8.20，每 commit 一 bump，semver 失效、CHANGELOG 退化为 commit log。
- `update_versions.php/.ps1` 为过时硬编码工具，建议删除。

## WordPress 插件规范符合性审查（用户追问）
- **结论：未完全符合**。正面项：插件头完整、文本域一致（WPCA_TEXT_DOMAIN='wp-clean-admin' 与头 Text Domain 匹配，全代码 0 处字面 'wpca' 域）、无危险函数(eval/exec/base64_decode/create_function 等 0 命中)、净化(27 文件 sanitize_*)与转义(21 文件 esc_*/wp_kses)覆盖广、激活/停用钩子齐全、DB 走 prepare。
- **规范级缺口**：
  - `Requires PHP: 7.0`（wp-clean-admin.php:15 + composer.json `>=7.0`）与代码实际不符（用 `: void`/`?int`/标量类型 → ≥7.1），建议改 7.4。
  - 8 个生产 PHP 文件缺 ABSPATH 守卫：includes/class-wpca-cache.php、cache-{api,database,file,memory}-tasks.php、class-wpca-error-handler.php、modules/core/classes/class-wpca-error-handler.php、modules/utilities/classes/class-wpca-cache.php（另有 3 个 tests/* 缺守卫但属 dev）。
  - 无 `register_uninstall_hook` / `uninstall.php`（卸载不清理选项/瞬时/能力）。
  - WPCS 未强制：`phpcs.xml.dist` 关 12 条 + 仅 3 条安全 sniff + CI `continue-on-error: true`；插件主动偏离 WP 命名(camelCase 方法、下划线前缀全局函数)，非严格 WP-CS 合规。
  - `Network: true` 已声明但未见多站点处理。
- **WP.org 发布硬门槛未达标**：缺 `readme.txt`（仅 README.md/en.md）；ABSPATH 守卫、Requires PHP 准确性、uninstall 清理。
- 修正过度概括：根版 `includes/ajax/*` 各 AJAX handler 已正确做 nonce+capability+sanitize（user-roles-ajax.php:38-90 为范例），安全缺口只在 modules 版 menu-customizer 副本。

## v1.9.0 全量修复（用户指令「修复所有」，提交 6651d8a 已推送）
- **P0 namespace（实际 24 个文件，非记忆中的 35）**：modules/admin/classes 20 个 + modules/utilities/classes 4 个，`namespace WPCleanAdmin;` → `...\Modules\Admin\Classes` / `...\Modules\Utilities\Classes`（与 module-loader 的 safe_init FQCN 和 autoload 目录映射一致）。改前验证过这批文件内 0 处对根版类的非限定引用，namespace 改名安全。
- **修复清单**：helpers.php 假 stubs require 删除；2FA 绕过两副本（根 trait + modules 类）改为返回 WP_Error('wpca_two_factor_required') + 验证码表单链接（原逻辑挂 login_redirect 后直接 return $user 放行）；modules menu-customizer 4 个 AJAX 注册改指新增的 ajax_save/reset/export/import 包装方法（新增 verify_ajax_request: nonce 'wpca_ajax_nonce' + manage_options；字段名 settings/import_data 与根版 ajax 类一致）；8 生产文件 + 3 测试文件补 ABSPATH 守卫；插件头 Requires PHP→7.4、移除 Network: true、Version/WPCA_VERSION→1.9.0；composer.json php>=7.4；新增 uninstall.php（显式选项 + LIKE 'wpca\_%' 清理 options/transients/usermeta）；新增 wpcleanadmin/readme.txt（WP.org 格式）；新增 tests/PluginAutoloadSmokeTest.php（27 个 FQCN 两套体系冒烟校验）。
- **执行技巧**：24 文件 namespace 批量修改用 Node 一次性脚本（精确子串替换，UTF-8 无 BOM，PowerShell Set-Content 会写 BOM 故不用）；被改文件头注释 @version 统一 bump 脚本内完成；临时脚本放 .codebuddy/tmp/ 用后即删。
- **待验证**：本机无 PHP，namespace 修复与 2FA/menu-customizer 改动需看 CI（php -l + phpunit 含新冒烟测试）结果。
- **提交卫生**：工作区存在会话间隙外部改动（AGENTS.md 被改、prototype/ui/* 被移到 prototype/ 根），未纳入本次提交，保持不动。
